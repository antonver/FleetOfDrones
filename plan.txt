получается что мы делаем: у нас есть дрон который запущен в контейнере, он не один а запущен в кубернетисе, он 
получает инфу на ендпоинте диспетчерской, 
тоесть есть приходит заказ от пользователя, у нас есть бд, гдезаписанны все данные о сколько дронов доступно, и где они сейчас находятся, 
дроны летают на реальной карте тоесть есть 3д карта. координатами, они все двигаются с одной скоростью, у нас есть скрипт который автоматически постояннойсоздает заказы,
на основе этих закаов наша модель по сборке данных может скать, во первых когда нужно больше заказов, и какая модельпостроения маршрута более еффективна, 
для этого нам нужны метрики, время, растояние, поогодные условия,например вот. Тоесть мы сделаем скрипт которые во время дождя больше заказов делает, и + 
выходной день или праздник у нас вот, и получается, 
нам нужен дрон который двигается по маршруту равномерно отдавая данные в диспетчерскую постоянно или в кафку напрямую,
+ диспетчерская которая постоянно принимает заказы и 


bd:

Table orders:

id: int
kg: float
model: string
type: food, notfood
final_point: coordianates postgres special fieald
orders_time: time
status: Delivared | Pending | is_being_delivered | Canceled
fleet: int(id)

Table drones:

id: int
version: float
model: string


Table drone_models:

id: int
name: string
battery: int
max_mass: float


Fleet:

id: int
city: string
country: string